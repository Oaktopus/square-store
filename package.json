{
  "name": "oaktopus-store",
  "version": "1.0.0",
  "private": true,
  "description": "OAKTOPUS store - static site + Cloudflare Pages Functions",
  "scripts": {
    "dev": "npx wrangler pages dev public",
    "deploy": "npx wrangler pages deploy public"
  }
}
